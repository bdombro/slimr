# Moved

This guide was renamed and slimmed down.

→ **[Auth listeners](./Auth.md)** — `onLogout`, `onAuthenticated`, callback matrix. React: **`db.auth.*$.use()`** on a **`DbSyncR`** instance from `@slimr/dbsync/react` (removed `useDbSession` / `useDbAuth`).

For app shell routing and getters, see **[Integration guide](./Offline.md)** and **[API reference](./API.md#dbauth-dbsyncauth)**.
